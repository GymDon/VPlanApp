<resources>

<!--         Will be done better, just to be reviewed, bugs or sth to me.   (LeMuecke) -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="android:actionBarTabStyle">@style/ActionBarTabView</item>
        <item name="android:actionBarTabBarStyle">@style/ActionBarTabBar</item>
    </style>

    <style name="ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/action_bar</item>
 <!-- #003D4D -->
    </style>

    <style name="ActionBarTabView" parent="@style/Widget.AppCompat.ActionBar.TabView">

        <!-- tab indicator -->
        <item name="android:background">@drawable/tab_bar_background</item>
    </style>
    
    <style name="ActionBarTabBar" parent="@style/Widget.AppCompat.ActionBar.TabBar">

        <!-- tab indicator -->
        <item name="android:background">@color/tab_highlight</item>
    </style>

</resources>